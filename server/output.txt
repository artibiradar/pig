server = load '/arti/logmodule.csv' using PigStorage(',') as (date,time,module:chararray,log:chararray,id:chararray);
dump server;
1.u = group server by log;
L = foreach u generate group as log,COUNT(server)as headcount;
dump L;
2.m = FILTER server by log=='[ERROR]';
c = group m by module;
f = foreach c generate group as module,COUNT(m) as headcount;
p = group f by module;
dump p;
a = foreach p generate MAX(f.$1);
3.c = group server by module;
l = foreach c generate group as module,COUNT(server) as headcount;
dump l;